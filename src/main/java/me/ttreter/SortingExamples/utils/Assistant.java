package me.ttreter.SortingExamples.utils;

public class Assistant {
    // Esta classe definitivamente é desnecessária, assim como algumas outroas aí, só criei porque sim.
    // Nesta varíavel está armazenada a opção do utilizado para o método de ordenação.
    protected static int METHOD = 0;
    // Nesta variável está armazenada o número de dados pedidos ao utilizador.
    protected static int AMMOUNT_OF_ITEMS;
    // Nesta variável está armazenada o vetor com os itens definidos pelo utilizador.
    protected static int[] ARRAY;
}
